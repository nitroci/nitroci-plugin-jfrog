version: 1
workspace:
  id: 3291554368a34ae38205a263489b1289
  name: nitroci-plugins-jfrog
commands:
  - name: build
    description: Build the command line.
    steps:
      - cwd: .
        scripts: 
          - mkdir -p ./dist && go build -o ./dist .
